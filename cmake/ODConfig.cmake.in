
set(OD_INCLUDE_DIRS ${CMAKE_INSTALL_PREFIX}/${OD_INSTALL_INCLUDE_DIR})
set(OD_LIBRARY_PATH ${CMAKE_INSTALL_PREFIX}/${OD_INSTALL_LIBRARY_DIR})
set(OD_LIBRARIES ${OD_INSTALLED_LIBRARIES})

if(WIN32) 
  if(MSVC) 
    set(CMAKE_CXX_LINK_FLAGS "${CMAKE_CXX_LINK_FLAGS} /SUBSYSTEM:WINDOWS /DNOMINMAX") 
  elseif(CMAKE_COMPILER_IS_GNUCXX)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -mwindows -std=c++11") 
  endif()
elseif(UNIX)
  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
endif()